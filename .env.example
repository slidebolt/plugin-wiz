# plugin-wiz environment
# For local discovery mode, only PLUGIN_DATA_DIR is required.
# The plugin uses UDP broadcast discovery (port 38899) to find Wiz bulbs.
# Copy to .env.local and customize with real values.

# Required: Directory for plugin state storage
PLUGIN_DATA_DIR=/tmp/plugin-wiz

# Optional: Plugin settings
PLUGIN_WIZ_ENABLED=true
PLUGIN_WIZ_LOG_LEVEL=info
