# Lock — Protocol Contract

> **Plugin authors:** Replace the "External Protocol" section below with the
> raw protocol binding for your plugin. The feature file at
> `features/lock.feature` tests the SlideBolt side; this document
> explains the external side.

## External Protocol

<!-- Replace this section. Examples by plugin type:
     - Zigbee2MQTT: paste the MQTT HA discovery YAML payload for this entity
     - ESPHome: show the YAML device config + REST curl examples
     - Wiz: show the SDK type definition or API call
     - HomeAssistant: show the entity config snippet
-->

_(No protocol binding defined yet. Add your plugin-specific content here.)_

## SlideBolt Domain Mapping

| External field | SlideBolt field |
|---------------|-----------------|
| (TBD)         | Lock.<field> |

## Supported Commands

| Command | SlideBolt action |
|---------|-----------------|
| (TBD)   | (TBD)           |

## Notes

_(Add any integration notes, caveats, or quirks specific to this entity type in your plugin.)_
